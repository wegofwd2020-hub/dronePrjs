"""engine.sensors — camera, IMU, depth, fiducial sensor abstractions.

Phase 2 introduces Protocols for the sensor types the mission planner
relies on: ``Camera`` (still capture), ``IMU``, ``DepthSensor``, and
``FiducialDetector`` (for closedSpace SLAM augmentation).
"""
