"""closedSpace — indoor / confined-airspace drone operations.

Sub-packages:

* :mod:`closedSpace.map` — warehouse map loading and validation.

See ``ISA.md`` and ``docs/map-schema.md`` for the spec.
"""
